# Copy to `.env` (git-ignored) or `export` these in your shell. Never commit real keys.

# Open-weight reproduction via OpenRouter (run_openrouter_baseline.py, prompt_sensitivity.py)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Original GPT-4o pipeline (vrtoolkit VLM step)
OPENAI_API_KEY=sk-your-key-here
OPENAI_VISION_MODEL=gpt-4o
OPENAI_TEXT_MODEL=gpt-4o
